---
/**
 * Layout principal de la aplicación Mueblería Iris
 *
 * - Este archivo define la estructura base para todas las páginas y vistas de la tienda.
 * - Incluye la importación de estilos globales.
 * - Renderiza la cabecera (Header), pie (Footer) y provee el contexto global de carrito a través de <CartProvider />
 * - Define metadatos para SEO y redes sociales, y estructura el head de forma dinámica.
 * - Reserva espacios para scripts y slots personalizados en el head.
 *
 * Uso: Todas las páginas principales heredan de este Layout para asegurar consistencia visual y funcional.
 */
import "../styles/global.css";
import Header from "../components/ui/Header.astro";
import Footer from "../components/ui/Footer.astro";
import CartProvider from "../components/cart/CartProvider";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalUrl?: string;
}

const {
  title = "Muebleria Iris - Muebles de Calidad para tu Hogar",
  description = "Descubre nuestra coleccion de muebles de alta calidad. Sofas, sillas, mesas, camas y mas. Envios a todo el pais.",
  image = "/og-image.jpg",
  canonicalUrl = Astro.url.href,
} = Astro.props;

const siteName = "Muebleria Iris";
const fullTitle = title.includes(siteName) ? title : `${title} | ${siteName}`;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{fullTitle}</title>
    <meta name="title" content={fullTitle} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:locale" content="es_AR" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalUrl} />
    <meta property="twitter:title" content={fullTitle} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={image} />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Additional head content -->
    <slot name="head" />
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- Skip to main content for accessibility -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-primary-600 focus:text-white focus:rounded-md"
    >
      Saltar al contenido principal
    </a>

    <Header />

    <main id="main-content" class="flex-1">
      <slot />
    </main>

    <Footer />

    <!-- Cart Drawer (React Island) -->
    <CartProvider client:load />

    <!-- Scripts -->
    <slot name="scripts" />
  </body>
</html>

<!--
==================================================
Flujo de trabajo y comunicación:

- Este Layout actúa como el esqueleto de toda la aplicación web, envolviendo cada página con cabecera (Header), pie (Footer) y gestionando estilos y metadatos.
- <CartProvider /> monta el contexto React para el carrito y provee funcionalidad global de e-commerce a cualquier página.
- Los componentes y páginas que usan este layout reciben acceso automático a la navegación principal, el pie de página y slots dinámicos para contenido y scripts.
- Define las etiquetas SEO tanto para motores de búsqueda como para previsualización en redes sociales.
- Es invocado desde todas las páginas Astro (por ejemplo, `index.astro`, `catalogo/index.astro`, etc.) y es responsable de la experiencia visual y funcional común a toda la web.
==================================================
-->
